import { StyleSheet, Text, View } from "react-native";
import React from "react";

const BankSelector = () => {
  return (
    <View>
      <Text>BankPicker</Text>
    </View>
  );
};

export default BankSelector;

const styles = StyleSheet.create({});
